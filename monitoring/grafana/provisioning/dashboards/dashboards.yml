apiVersion: 1

providers:
  - name: EduMind
    orgId: 1
    folder: EduMind
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards
