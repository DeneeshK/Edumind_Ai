# EduMind backend dependencies
#
# Supported Python: 3.10-3.12
#
# This is intentionally a clean application requirements file, not a full
# `pip freeze` dump. It pins the risky parts of the stack and avoids unrelated
# GPU/training packages that commonly create resolver conflicts.

# Web API
fastapi==0.111.1
uvicorn[standard]==0.30.6
slowapi==0.1.9

# Auth
google-auth==2.53.0
python-jose==3.5.0

# Data validation and settings
pydantic==2.7.4
# Bumped from 2.3.4: the `mcp` client SDK (used to reach the web-search MCP
# server) requires pydantic-settings>=2.5.2. Compatible with pydantic 2.7.4.
pydantic-settings>=2.5.2,<3

# MCP client — reaches the standalone edumind_mcp_search server for web-search RAG.
mcp>=1.2.0

# Database
asyncpg==0.29.0

# LLM and search clients
groq==0.9.0
tavily-python==0.3.9
# Groq 0.9.0 still passes the legacy `proxies` argument internally; httpx
# removed that keyword in 0.28, so keep this pinned until Groq is upgraded.
httpx==0.27.2

# NOTE: chromadb, sentence-transformers, transformers, torch, numpy, scipy and
# scikit-learn were removed here after the local ChromaDB/BGE retrieval path was
# deleted — nothing in this backend imports them anymore. Web-search RAG now runs
# in the standalone edumind_mcp_search server, which pins its own dependencies.

# Scheduler, utilities, logging
APScheduler==3.10.4
python-dotenv==1.0.1
loguru==0.7.2

# Tests
pytest==8.2.2
pytest-asyncio==0.23.8

#prometheus
prometheus-client==0.17.0